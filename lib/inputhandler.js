class InputHandler {
  constructor() {
  }

  checkInput(){
  /*
    var input = null;
    window.addEventListener('keydown',(e)=>{
      switch (e.keyCode) {
        case 37: case 65:
          //left arrow and 'S' key
          input = "left";
        return input;
        case 39: case 68:
          //right arrow and 'D' key
          input = "right";
        return input;
        case 38: case 87:
          //up arrow and 'W' key
          input = "up"
        return input;
        case 40: case 83:
          //down arrow and 'S' key
          input = "down";
        return input;
        case 32:
          //spacebar
          input = "space";
        return input;
        default:
          input = null
        return input;
      }
    });
    if(input != null){
      console.log(input);
      return input;
    }
  }
  */
}
